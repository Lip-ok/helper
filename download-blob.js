// const blob = new Blob([response], {type: "application/octet-stream"});
//
// const file = new Blob([response], {type: 'application/octet-stream'});
// console.log('file', file);
// FileSaver.saveAs(file, `document.${ext}`);
// FileSaver.saveAs(file, `document.docx`);


// const file = new File([res.data], `document.${ext[1]}`, {type: `application/${ext[0]};charset=utf-8`});
// FileSaver.saveAs(file);

// const file = new File([res.data], "document.docx", {type: "text/plain;charset=utf-8"});
// FileSaver.saveAs(file);

// const blob = response.data;
// const file = new File([blob], `document.${ext}`, {type: "application/octet-stream"});
// FileSaver.saveAs(file);